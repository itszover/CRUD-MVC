extends ../layout

block content
    div.readmore-container
        div.readmore
            h1 #{post.title}
            small #{post.createdAt.toLocaleString()}
            a(href="/posts"): button(type="button") Ver todas
            h3 #{post.description}
            p #{post.text}